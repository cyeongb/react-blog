let Router,postsCtrl;_610‍.x([["default",()=>_610‍.o]]);_610‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_610‍.w("./posts.ctrl",[["*",null,function(v){postsCtrl=v}]]);

//import { list, write, read, remove, replace, update } from './posts.ctrl'; //posts.ctrl 에서 export 한 것들을 require 함수로 가져옴

const posts = new Router();

const printInfo = (ctx) => {
  ctx.body = {
    method: ctx.method,
    path: ctx.path,
    params: ctx.params,
  };
};

// 해당 경로에 해당하면 printInfo 함수를 호출한다.
posts.get('/', postsCtrl.list);
posts.post('/', postsCtrl.write);
posts.get('/:id', postsCtrl.read);
posts.delete('/:id', postsCtrl.remove);
posts.patch('/:id', postsCtrl.update);

_610‍.d(posts);
